<Window x:Class="LifeLight.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LifeLight"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="LifeLight" Height="600" Width="1200">
    <Window.Resources>
        <Style  TargetType="{x:Type ListBoxItem}">
            <Style.Triggers>
                <!--<Trigger Property="ItemsControl.AlternationIndex" Value="0">
                    <Setter Property="Background" Value="LightGray"></Setter>
                </Trigger>-->
                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                    <Setter Property="Background" Value="#F5F5F5"></Setter>
                </Trigger>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="IsSelected" Value="True" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Right" Margin="10,0">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="0,0,0,5" />
                </Style>
            </StackPanel.Resources>
            <Calendar Name="calDate" SelectedDatesChanged="calDate_SelectedDateChanged" />
            <!--<DatePicker Name="dpDate" SelectedDateChanged="dpDate_SelectedDateChanged" />-->
            <Button Name="btnEdit" Click="btnEdit_Click">Edit</Button>
            <Button Name="btnExport" Click="btnEdit_Click">Export</Button>
            <TextBlock Text="TODO: Add MCT, Olive Oil, Iodine" />
            <TextBlock Text="TODO: Add optional list items with previous date, (bath)" />
            <TextBlock Text="TODO: Add mood rating trackbar" />
            <TextBlock Text="TODO: Add daily comments textbox" />
        </StackPanel>
        <ListBox Name="TodoList" HorizontalContentAlignment="Stretch" SelectionMode="Extended" AlternationCount="2"><!--SelectionChanged="lbTodoList_SelectionChanged">-->
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="240" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="120*" />
                        </Grid.ColumnDefinitions>
                        <CheckBox IsChecked="{Binding Complete}">
                            <TextBlock Text="{Binding Title}" />
                        </CheckBox>
                        <Button Grid.Column="1" Visibility="{Binding TimeVisibility}" Width="60" Name="btnSetNow" Click="btnSetNow_Click">Set Now</Button>
                        <TextBox Grid.Column="2" Visibility="{Binding TimeVisibility}" Name="tpSetTime" Text="{Binding Time}" />
                        <TextBox Grid.Column="3" Text="{Binding Comment}" Margin="20,0,0,0" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</Window>
